# wrangler.toml
name = "pranitaladdhaprofile"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]
main = "public/_worker.js"

[assets]
directory = "dist"

# Bindings (like KV) should be set in the Cloudflare Dashboard
# to avoid hardcoding IDs in this file.
